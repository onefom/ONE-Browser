# One Browser 1.8.15 Windows x64 测试状态

构建日期：2026-09-18

## 已完成

- Windows 系统代理优先；未启用系统代理时自动回退直连。
- fingerprint-chromium 下载与 Clash 订阅导入优先使用系统代理。
- 当前 Wails 壳层接管关闭事件，点击右上角关闭按钮仅隐藏到托盘，只有托盘菜单能够退出应用。
- Windows WebView GPU 已启用；最大化和连续调整窗口尺寸时暂停毛玻璃动画与昂贵重绘。
- 代理节点只保留一个委托式点击监听，并加入单次删除锁。
- 删除当前代理节点后，界面自动恢复“系统代理优先”。
- 活跃界面和桌面桥接仅保留 fingerprint-chromium Windows x64。
- 工作台起始页展示窗口配置并实时检测出口 IP，第二标签打开 Google。
- 移除 Ant 指纹检测书签，并加入 `--no-default-browser-check`。
- 三个内置扩展会按启用状态下载安装，并设为新窗口默认扩展。
- Chrome 应用商店和 Google Drive 在运行中的受管浏览器标签页打开。
- 操作日志记录前端关键操作与后端错误，支持导出、30天清理及当前列表清空。
- 便携包不包含浏览器内核、账号、代理节点、日志、缓存或用户数据库。
- 便携包附带 Windows x64 Mihomo 代理运行时，用于启动用户导入的 Clash 节点。
- 代理删除显示为 X；筛选按钮取消外描边。
- Windows 主窗口、EXE 与托盘统一为 One Browser O 图标资源。
- 工作台出口 IP 与网络位置合并为同一信息组。
- Chromium 扩展开发者模式默认开启；持久扩展注册明确写入启用状态。
- 账号列表不再提供密码预览；侧栏账号区和更多按钮间距已调整。
- 系统设置右上角显示 1.8.15 版本号；帮助中心同步关闭到托盘、云备份浮层和 Esc 返回说明。
- One Browser 主界面和工作台均优先使用苹方字体。
- 每个窗口配置使用独立用户数据目录、调试端口并强制 `--new-window`，防止第二个配置复用第一个浏览器窗口。
- 工作台已改为紧凑指纹信息表，显示完整 User Agent、语言、时区、地理位置策略、分辨率、字体指纹和 WebRTC 状态。
- Wails WebView2 用户目录固定为 `data/webview2`，账号、窗口、界面设置和本地浏览器状态不再继续写入 AppData。
- 空白便携包不再自动导入旧版 AppData WebView2 目录；已有便携数据仍不会被覆盖。
- 文件日志强制写入 `data/logs/app.log`，启用滚动并将保留期限制为30天。
- 新建窗口弹窗隐藏滚动条，使用底部渐变下滑提示；取消和创建按钮固定在滚动区外。
- 下滑箭头和文字上下居中，遮罩改为更明显的浅紫色渐变。
- 内核升级红点只由已安装版本和当前版本差异触发；无内核时明确提示先下载。
- 系统日志页展示完整内存日志，并提供 ALL、DEBUG、INFO、WARN、ERROR 五级实时筛选。
- 新建窗口下滑提示扩大箭头与文字间距；“查看配置”提供完整二级详情弹窗。
- 窗口表格列宽与启动按钮宽度固定，启动状态变化不再引起前方列位移。
- 工作台新增网络、代理、账号、扩展、隔离与工作区摘要，并继续使用紧凑小字号。
- 默认浏览器窗口以 `--window-size=900,680 --window-position=12,12` 在左上角启动。
- User Agent 推荐项已统一，自定义项提供输入框并真实写入 Chromium 启动参数。
- 内核页支持导入本地归档或已解压目录；系统设置新增缓存清理、配置导入导出和保留内核的系统初始化。
- 窗口统计卡片与表格左右边界统一，卡片阴影改为柔和低对比效果。
- 账号、代理、插件和内核页移除重复二级标题，页面说明统一放入顶部标题区。
- 全部下拉控件改为不受滚动容器裁切的圆角浮层；高级设置图标统一为线性 SVG。
- 内核下载增加分段长度校验、六次重试、断点续传和单流回退，并通过主动截断连接测试。
- 升级与云备份入口只保留在窗口管理首页；侧栏顺序统一为窗口、内核、代理、账号、插件、日志、设置。
- 窗口表格三个配置图标改为点击操作，移除被表格裁切的悬浮黑条；更多按钮与启动按钮垂直居中。
- 账号、代理与插件页操作按钮统一为 40px，并与页面标题同一顶部节奏；页面内容起点与内核页一致。
- 内置扩展下载改为后台准备，不再阻塞浏览器窗口启动。
- 系统日志页明确只显示当前进程会话；磁盘日志路径为 `data/logs/app.log`。
- 首页网络详情通过对应窗口的真实 Profile/代理栈同步出口 IP 与位置，不再使用演示占位数据。
- 代理节点卡片点击即选中，显示真实测速延迟或 Timeout，并提供手动刷新与代理页可见时的 60 秒自动刷新。
- 首页高级筛选支持内核版本、分组和代理情况；帮助中心固定标题与左侧导航，仅右侧内容滚动。
- 代理导入弹窗隐藏可视滚动条，删除 X 按钮整体缩小 10%，节点选中与强调色跟随当前主题。
- One Browser 导入节点不再强制绑定 Mihomo；旧节点在测速或启动时自动改为跟随当前连接栈。
- 再次点击当前代理节点会取消选择并恢复系统代理；Mihomo 运行时可识别便携包内 compatible 文件名。
- 筛选按钮移除箭头并使用主题底色，筛选面板固定在按钮正下方。
- 三种代理导入页面共用缩小 10% 的固定窗口尺寸；帮助内容可滚动但不显示滚动条。
- 首页云备份改为云按钮下方的锚定浮层，不再使用独立全屏弹窗。
- 代理管理和系统日志固定顶部区域，仅下方内容滚动，且不显示可视滚动条。
- 侧栏展开与折叠状态统一使用 28px 账号头像，并固定底部分隔线和三行布局。
- 创建窗口、代理导入、配置详情、账号、内核、初始化、备份浮层和浏览器预览均支持 Esc 返回。

## 已通过的检查

- 前端 TypeScript 编译与 Vite production build。
- JavaScript 语法检查。
- 代理删除模拟点击：连续两次点击只触发一次删除调用。
- 全量 `go test ./...` Go 单元测试。
- 全量 `go vet ./...` 静态检查。
- Wails Windows/amd64 production 交叉构建。
- PE 文件检查：主程序与 Mihomo 均为 PE32+ x86-64。

## 已知限制

- 当前执行环境为 Linux，没有 Windows 或 Wine，因此不能在此环境真实点击 Windows 原生关闭/最大化按钮，也不能读取真实 WinINet 注册表或启动 WebView2。
因此本包是已成功构建、通过全量自动化测试的 Windows x64 便携候选版，但不是 Windows 实机验收版。首次在 Windows 11 运行时，建议重点验证同时启动两个窗口时是否各自打开独立浏览器，以及关闭、最大化、系统代理和 fingerprint-chromium 下载。
