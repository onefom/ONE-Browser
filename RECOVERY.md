# One Browser 恢复代码包

创建时间：2026-09-12

此包用于在网络恢复后继续制作 Windows x64 绿色版。请保留整个 ZIP，不要修改内部目录结构。

当前已保留：

- 完整界面源代码（`dist/`）与最新深色模式、帮助中心、账号、代理、内核、插件和备份界面。
- Windows 桌面端外壳（`desktop/`）：主窗口、预加载桥接、数据目录和“工作台配置 + Google”双标签窗口。
- `package.json`：Windows x64 打包依赖与打包命令。
- 默认首次启动状态：没有账号、没有浏览器内核、没有窗口数据。
- `README-Windows.txt`：绿色版的使用与数据目录说明。

恢复方法：

1. 将 ZIP 上传到对话中。
2. 说明“继续 One Browser Windows 绿色版打包”。
3. 我会安装必要依赖、进行 Windows x64 打包、检查绿色版目录和使用文档，再交付最终 ZIP。

注意：

- `data/` 为运行时本地数据目录，不包含在恢复代码包中。
- 浏览器内核也不包含在恢复代码包中，保持初始包体较小。
